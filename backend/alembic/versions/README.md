# ============================================================
# Alembic Versions Directory
# ------------------------------------------------------------
# This folder holds the actual migration SCRIPT files, one per
# schema change. Alembic numbers them in order (revisions).
#
# Currently empty because Phase 1 creates tables directly via
# `Base.metadata.create_all`. We'll add the first real migration
# here shortly so the schema is version-controlled.
# ============================================================
