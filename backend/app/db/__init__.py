# ============================================================
# Lentis Gallery — Database Package
# ------------------------------------------------------------
# "db" (database) holds everything that connects the app to
# PostgreSQL: the SQLAlchemy engine, the session factory, and the
# Base class that all our table models will inherit from.
# ============================================================
