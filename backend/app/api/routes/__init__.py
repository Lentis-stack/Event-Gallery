# ============================================================
# Lentis Gallery — API Routes Package
# ------------------------------------------------------------
# Each route file (health.py, auth.py, events.py, ...) lives in
# this folder. This __init__.py marks the folder as a package so
# we can import `from app.api.routes.health import router`.
# ============================================================
