# ============================================================
# Lentis Gallery — API Package
# ------------------------------------------------------------
# "api" holds all the HTTP endpoint routers (route definitions).
# Each domain (auth, events, guests, media, hosts, admin) gets
# its own router file. We register them all in main.py.
# ============================================================
