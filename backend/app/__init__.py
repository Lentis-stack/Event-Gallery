# ============================================================
# Lentis Gallery — Backend Package
# ------------------------------------------------------------
# This __init__.py file tells Python that the "app" folder is a
# package. A "package" is just a folder of related Python modules
# that we can import from. Without this file, Python wouldn't let
# us do `from app.core.config import settings`.
#
# These files are intentionally empty (or near-empty) — they exist
# mainly to mark folders as importable packages.
# ============================================================
